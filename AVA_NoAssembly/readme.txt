The dependencies of these processors are unfortunately old style 'code in Assets' packages.
Supporting them is only possible by also importing this folder into your 'Assets' folder.
There is no way to do this legitimately. Srsly ChilloutVR, at least use assemblies, plz.